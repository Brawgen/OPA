#pragma once
#include <queue>
#include <iostream>
#include <chrono>
#include <thread>
using namespace std;
void work(std::queue<int> queue1, std::queue<int> queue2, std::queue<int> queue3, bool k1, bool k2, bool k3);